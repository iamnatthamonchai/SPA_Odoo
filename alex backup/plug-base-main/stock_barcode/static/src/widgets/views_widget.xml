<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="stock_barcode_views_widget">
        <div>
            <div class="o_barcode_generic_view"></div>
            <div class="fixed-bottom" t-if="widget.mode == 'edit' &amp;&amp; widget.view_type === 'form'">
                <div class="o_barcode_control d-flex">
                    <button class="btn btn-secondary o_discard">Discard</button>
                    <button class="btn btn-primary o_save">Confirm</button>
                </div>
                <div t-if="widget.canBeDeleted === true" class="o_barcode_control d-flex">
                    <button class="btn btn-danger text-uppercase o_delete"><i class="fa fa-trash-o mr-1"/>Delete</button>
                </div>
            </div>
        </div>
    </t>

</templates>